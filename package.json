{
  "name": "@kmerhosting/api",
  "version": "0.1.0",
  "private": true,
  "description": "Public, versioned API gateway for KmerHosting.",
  "type": "module",
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test",
    "typecheck": "bun x tsc --noEmit"
  },
  "dependencies": {
    "swagger-ui-dist": "5.17.14"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "5.7.3"
  }
}
