server {
    listen 80;
    listen [::]:80;
    server_name api.kmerhosting.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name api.kmerhosting.com;

    ssl_certificate /etc/letsencrypt/live/api.kmerhosting.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.kmerhosting.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    client_max_body_size 64k;
    add_header X-Content-Type-Options nosniff always;
    add_header Referrer-Policy no-referrer always;

    location / {
        proxy_pass http://127.0.0.1:8790;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        # Never forward a client-supplied chain to the API. The application
        # treats this value as the authenticated source IPv4 for allowlists.
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-Id $request_id;
        proxy_set_header Authorization $http_authorization;
    }
}
